#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two numbers
 * @a: first parameter to be multiplied
 * @b: second parameter to be multiplied
 * Return: result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
