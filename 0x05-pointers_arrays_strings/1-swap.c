#include "main.h"

/**
 * swap_int- swap the value of two intigers
 * @a: a first variable
 * @b: the second variable
 * return: nothing
 */
void swap_int(int *a, int *b);
{
	swap = *a;
	*a = *b;
	*b = swap;
}
