#include "main.h"

/**
 * _puts- prints a string
 * @str: the string variable
 * Return: nothing
 */
void _puts(char *str)
{
	int l = 0;

	while (*s != '\0')
	{
		_putchar(*str++);
	}
	_putchar('\n');
}
